import { MettreEnGrasDirective } from './mettre-en-gras.directive';

describe('MettreEnGrasDirective', () => {
  it('should create an instance', () => {
    const directive = new MettreEnGrasDirective();
    expect(directive).toBeTruthy();
  });
});
